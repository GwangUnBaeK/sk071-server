name: CI/CD Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'src/main/**'
      - 'k8s/*.yaml'
  pull_request:
    branches:
      - main
    paths:
      - 'src/main/**'

permissions:
  contents: write

jobs:
  ci:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build check
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false

  check-changes:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      src: ${{ steps.filter.outputs.src }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            src:
              - 'src/main/**'

  build:
    needs: check-changes
    if: |
      github.event_name == 'push' &&
      needs.check-changes.outputs.src == 'true'
    runs-on: ubuntu-latest
    outputs:
      k8s_updated: ${{ steps.update_k8s.outputs.k8s_updated }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/sk071-server:${{ github.sha }}

      - name: Update k8s image tag
        id: update_k8s
        run: |
          sed -i "s|image: gwangun/sk071-server:.*|image: gwangun/sk071-server:${{ github.sha }}|g" k8s/deploy.yaml
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add k8s/deploy.yaml
          git commit -m "Update image tag to ${{ github.sha }}"
          git push
          echo "k8s_updated=true" >> $GITHUB_OUTPUT

  deploy:
    needs: build
    if: |
      github.event_name == 'push' &&
      needs.build.outputs.k8s_updated == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get commit messages
        id: commits
        run: |
          COMMITS=$(git log --pretty=format:"- %s" -10)
          echo "messages<<EOF" >> $GITHUB_OUTPUT
          echo "$COMMITS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Get current datetime
        id: datetime
        run: echo "datetime=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Create Release Note
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: release-${{ steps.datetime.outputs.datetime }}
          release_name: release-${{ steps.datetime.outputs.datetime }}
          body: |
            ## 변경된 커밋 목록
            ${{ steps.commits.outputs.messages }}
